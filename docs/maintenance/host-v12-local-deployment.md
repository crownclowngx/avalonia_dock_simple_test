# V12 单文件部署与手工影响范围

> 日期：2026-09-17。重构源码提交：`85a3bfee8da966c49ee3288152b8845946ae3b56`。
> 用户已授权提交 Git、编译并部署到 `D:\data\avalonia`，随后自行手工验收。构建源码、安装 EXE、备份位置与完成状态以[本次部署证据](../archive/records/host-v12/local-deployment-20260917.json)为准。

## 1. 改动与影响范围

| 范围 | 实际改动 | 可能受影响的操作 | 预期保持的行为 |
| --- | --- | --- | --- |
| 插件注册与启动 | 防御性声明快照、局部 Validator、全局 ConflictAnalyzer；Builder 保留最终提交 | 启动加载插件，功能目录与工具目录生成，重复／无效声明的隔离及诊断 | 有效插件正常出现；失败插件隔离；所有者、诊断顺序与 Scope/Provider 寿命不变 |
| 命令展示刷新 | Menu、KeyBinding、Palette、Command 各自使用 UiRefreshScheduler | 菜单启用状态、快捷键、命令面板、活动文档切换、窗口关闭后的通知 | Menu/KeyBinding/Command 在 UI 线程即时更新，Palette 排队合并；释放后不再接收迟到通知 |
| 页面与工具查询 | 每次查询构造 WorkspaceLayoutQuerySnapshot，以对象引用建立关系索引 | 搜索已打开页面、定位同名页面、分割／浮窗、工具隐藏与固定、工具中心状态 | 使用原实例；浮窗与主树归属正确；Hidden 优先级不变；每次执行重新检查状态 |

SDK public API、插件包依赖、产品版本、稳定 ID、manifest V2、Document V2、Layout V3、用户数据位置和业务插件实现未调整。源码只改 Host 内部职责及测试、文档。布局、焦点、关闭与重启属于间接受影响链路，需要用安装版确认实际体验。

## 2. 手工检查表

从 `D:\data\avalonia\MyAvaloniaManagement.exe` 启动。优先使用可丢弃的测试文档；下面的预期均为既有行为。记录通过／失败、操作顺序和现象，出现问题时附页面类型、插件名称及是否浮窗，便于对应到具体链路。

| 编号 | 操作步骤 | 预期结果 | 关联范围 |
| --- | --- | --- | --- |
| M01 | 用现有插件目录启动；打开插件看板、功能中心和工具中心；任选常用插件创建页面与工具 | 启动正常；原有可用插件和入口存在；没有新增注册失败或重复条目 | 注册与启动 |
| M02 | 打开两个可保存页面，分别修改并切换；观察保存菜单，使用 Ctrl+S；再检查插件自己声明的菜单和快捷键 | 启用状态及时变化；只作用于当前页面；一次操作只执行一次 | 命令刷新、活动目标 |
| M03 | Ctrl+Shift+P 打开命令面板；快速输入、清空搜索，切换页面后再次打开；连续开关面板 | 功能、页面、工具、命令结果正确；无重复项、陈旧状态、卡死或关闭后报错 | Palette 排队合并、查询 |
| M04 | 打开两个同名页面并写入不同内容；分别从命令面板定位；关闭一个后新建同名页面 | 始终定位选中的原实例，内容不串页；已关闭项消失，新页面不冒充旧实例 | 页面引用身份与序号 |
| M05 | 把页面分割、浮动到两个窗口；最小化浮窗后从面板定位其中页面；再回停主窗 | 定位到正确窗口和分组；最小化窗口恢复；模型、编辑内容及焦点行为保持 | 分组与浮窗归属 |
| M06 | 在工具中心对常用工具依次隐藏、显示、四向固定／自动收起、浮动和回停；每次观察状态与搜索结果 | 状态与实际布局一致；隐藏后能找回；恢复原工具内容；重复查询不创建额外实例 | 工具关系与显隐优先级 |
| M07 | 对有未保存内容的页面发起关闭并取消，再确认关闭；重复关闭／重开工具中心及命令面板；最后正常退出 | 取消保留内容和操作能力；确认关闭后条目撤回；无重复关闭、迟到刷新异常或退出挂起 | 关闭时状态重查、退订与释放 |
| M08 | 调整工具位置、浮窗和隐藏状态后退出重启；如需测试重置布局，先记录当前布局 | 工具布局按原 V3 规则恢复；重置期间文档内容及原实例不丢失；Document 不跨启动自动恢复 | 查询与既有布局链路 |

可选：若现有测试插件有启动失败或可用性撤回场景，确认条目和说明及时变化、失效命令不能继续执行。业务下载、账号、数据库、视频／WebView 是否正常，应按各插件自己的检查表验证；Host 自动化没有代替这部分手工结果。

## 3. 构建与本机部署

重构完整开发 `verify` 已通过 1,095 项测试，新增 36 项，零失败、零跳过、构建零警告；SDK API 比较显示三个契约程序集的公开表面一致。详情见[开发记录](../archive/records/host-v12/development-acceptance.md)和[开发证据](../archive/records/host-v12/final-development-evidence.json)。本页是此后的文档补充，不改变已验证生产代码。

发布采用 `Release`、`win-x64`、`SelfContained=true`、`PublishSingleFile=true`、`IncludeNativeLibrariesForSelfExtract=true`、`EnableCompressionInSingleFile=true`、`PublishTrimmed=false`、`SkipPluginDeploy=true`。Host 与 .NET 运行时打入一个 EXE，HelpWeb 仍为帮助资源，Controls 仍为独立插件目录。

1. 从干净提交用 git archive 导出隔离源码，RID restore 和 publish 不改主工作区依赖锁。
2. 核对并复制已验证的 Avalonia 布局补丁与固定 Dock 本地包。发布阶段使用零警告构建，记录实际源码 SHA。
3. 在暂存目录核对 bundle、运行时与两项补丁身份；用隔离数据、空插件目录验证启动及正常退出。此步骤属于本机交付检查，不调用 Windows CI、seal 或正式发布门禁。
4. 记录安装目录与 Controls 的文件摘要和目录树；确认安装实例关闭后，将被替换文件备份到 `D:\data\avalonia-host-backups` 本轮专用目录，再核对暂存文件并替换。
5. 对照交付文件和保留文件逐项核验，回填非嵌入 JSON 并提交；最终部署证据不改变已打包的代码和 Markdown。

## 4. 回退与证据边界

回退前关闭安装实例，使用部署证据中的备份目录和文件清单恢复被替换文件，核对原 SHA-256。保留 Controls 和用户数据，不递归清理安装目录。回退撤销的是本轮 Host 更新，不恢复手工测试过程中自行修改的文档或布局。

本轮不使用 AIFLOW，不上传包、不推送 Git、不创建公开发布。M01–M08 留给用户在安装版操作，构建和隔离启动通过不能代替手工验收。首次[开发记录](../archive/records/host-v12/development-acceptance.md)中的“尚未提交／未部署”描述的是当时状态，后续提交与本机安装以本页所链接的部署证据为准。
