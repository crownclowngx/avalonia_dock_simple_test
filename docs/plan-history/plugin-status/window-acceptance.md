# 插件状态独立窗口实施验收

日期：2026-09-11。实现方案：[插件状态窗口设计](../../design/plugin-status-window.md)。

## 实施内容

完成独立非模态单实例窗口、菜单与命令面板统一入口、概览/搜索/筛选/详情/诊断复制、按需刷新，
以及旧插件状态 Tool 注册删除。布局和工具偏好共用退役白名单，并保留其他有效工具与缺失插件数据。
主界面、架构说明、工具中心说明、布局规范及文档导航已同步。

生产对象按查询、文案、展示模型、窗口所有权和命令入口分工；新增代码使用中文注释，解释快照、
选择保留、剪贴板异步反馈与迁移边界。SDK 和磁盘 schema 保持不变。

## 本地验证

最终 `Gate verify` 全部通过，运行编号 `20260911-090714-45082766c58a`，范围 `all`。
锁定还原、严格 Release 构建、测试、架构/契约检查、本地打包和实体包消费验收六个阶段均通过，
构建零警告、零错误；测试共 **791/791** 通过，无失败或跳过。

| 套件 | 通过 / 总数 |
| --- | --- |
| Plugin SDK | 91 / 91 |
| Host 单元测试 | 386 / 386 |
| Host 插件集成 | 211 / 211 |
| Headless UI | 91 / 91 |
| MyPlugTest | 11 / 11 |
| MyPlugTest 实体包验收 | 1 / 1 |

本地机器可核查 `artifacts/gate/20260911-090714-45082766c58a/summary.json` 及同目录 `pass-1/tests/` 下的 TRX。
专项窗口与工具中心回归另有 13/13 通过的结果，存放于 `artifacts/plugin-status-validation/window-regression.trx`。
旧工具中心虚拟化测试原来的两处 103 项断言已随内置 Tool 从三个减少到两个调整为 102 项，
其余实例创建、图标来源、隐藏不释放和虚拟化断言均保留。

已生成并人工检查浅色、深色、680 × 520 紧凑窗口和长诊断滚动截图，位于工作区
`artifacts/plugin-status-validation/screenshots/`。这些截图使用测试样例插件数据，不代表用户安装清单。
渲染使用 Avalonia Headless 与 Skia，不调用 Windows 桌面自动化。

文档内部链接和 `git diff --check` 均已检查通过。门禁完成后仅补记本文的实际验证结果。
本轮遵循用户要求，没有使用 AIFLOW、Windows CI、Windows Smoke 或 `Gate seal`/发布门禁；不产生发布资格。
